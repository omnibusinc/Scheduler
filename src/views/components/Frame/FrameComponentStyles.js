import basicStyles from '../../../styles/app';
const styles = Object.assign({}, basicStyles, {
  wrapper: Object.assign({}, basicStyles.root, {
    margin: '0 auto',
    maxWidth: 1440,
    marginTop: 30
  })
});

export default styles;